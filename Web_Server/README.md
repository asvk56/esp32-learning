# 10 Web Server

* **Concept:** Application layer HTTP protocol hosting and local network client orchestration.
* **Hardware:** Standalone ESP32 driving the native internal on-board blue status LED (GPIO 2).
* **Learning:** Initializing standard port 80 HTTP server stacks, parsing asynchronous client request strings, and embedding CSS-styled HTML UI directly inside microcontroller runtime memory.
