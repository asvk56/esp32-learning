# 09 Wi-Fi Connect

* **Concept:** Network Layer Initialization and 802.11 b/g/n Station (STA) mode architecture.
* **Hardware:** Standalone ESP32 utilizing internal RF network stack (No external peripheral required).
* **Learning:** Initializing the native `WiFi.h` library, managing blocking connection state machines, and acquiring local IP allocation via DHCP.
